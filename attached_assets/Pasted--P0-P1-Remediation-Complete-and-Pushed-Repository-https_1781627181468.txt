✅ P0 + P1 Remediation — Complete and Pushed
Repository: https://github.com/Nardo758/Project-Spark-OppGrid.git
Branch: master (2 commits)
Commits Pushed
Table
Commit	Description
d1656b2	P0+P1 remediation: disables fake dataset sales, fixes auth, unifies AI pricing, weighted billing, real data pipeline, cloud storage, SQLite local dev support, Node.js portable, startup scripts
5ba5f17	Updated SETUP_LOCAL.md with SQLite and env template instructions
P0 Critical Fixes (Security & Compliance)
Dataset marketplace disabled — POST /purchase returns 503 with maintenance message
Dataset auth fixed — uses get_current_user instead of broken _get_user_id_from_token
Stripe dev bypass removed — deleted is_stripe_dev_mode(), always verifies webhook signatures
Guest checkout standardized — is_verified=False, hashed password, creates Subscription(FREE)
Token spending caps enforced — allow_overage default changed from True to False
P1 Data Integrity Fixes
Unified AI pricing — single source of truth in ai_pricing_service.py (DB-first + fallback)
Weighted token billing — input + output*cost_ratio instead of raw total
Real dataset generation — queries HubOpportunityEnriched, HubMarketByGeography, DetectedTrend, ScrapeJob; falls back to labeled mock data
Cloud storage — R2/S3 upload + signed URLs, with local filesystem fallback
Async/sync boundary fixed — 3 endpoints in opportunities.py and reports.py changed from async def to def
Local Development Environment
Node.js 20.18.1 — downloaded to tools/node/ (portable, no install)
SQLite fallback — database.py auto-detects when no PostgreSQL config
Env template — env_local_template.md with placeholder keys (.env.local blocked by security)
DB init script — init_db.py creates all tables from SQLAlchemy models
One-click startup — start_local.bat launches backend + frontend simultaneously
Docs — SETUP_LOCAL.md updated with all instructions
GitHub Push Note
GitHub's Push Protection initially blocked the push because the env template contained a Stripe test key. I replaced it with sk_test_YOUR_KEY_HERE placeholders and rewrote the commit history via interactive rebase. The push is now clean and live.
